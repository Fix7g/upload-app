{% extends 'base.html.twig' %}

{% block title %}Zadanie{% endblock %}

{% block body %}


<div class="container">
        <table>
    {% for user in users %}
    
    <tr>
        <td>
            {{ user.name}} 
        </td>
        <td>
            {{ user.sname}}
        </td>
        <td>
            <a target="_blank" href="/showFile/{{user.id}}">Plik</a>
        </td>
      
    </tr>   
    {% endfor %}
</table>
</div>
{% endblock %}
